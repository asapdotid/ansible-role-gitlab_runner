{% if (gitlab_runner.docker_services is defined) and (gitlab_runner.docker_services | length > 0) %}
{% for service in gitlab_runner.docker_services %}
    [[runners.docker.services]]
{% for attr in service %}
      {{ attr }} = {{ service[attr] | to_json }}
{% endfor %}
{% endfor %}
{% endif %}
